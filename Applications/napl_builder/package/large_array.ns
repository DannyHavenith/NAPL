function main( args)
{
	size = 500000;
	arr = new Array();
	for (x = 0; x < size; ++x)
	{
		arr.push( x);
	}
	writeln( "finished len = " + arr.length());
	for (x = size - 1; x >=0 ; --x)
	{
		writeln( arr[x].toString());
	}
}